<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

## Вопросы для формирования промпта

### Структура катсцен

- Из каких элементов должна состоять катсцена? (диалоги, движение камеры, анимации персонажей, эффекты, звуки?)
- Как должны выполняться действия: последовательно, параллельно или с возможностью обоих вариантов?
- Нужна ли система условий и ветвлений в катсценах?


### Интерфейс редактора

- Как ты видишь визуальную часть: timeline-редактор (как в Adobe After Effects), нодовый редактор (как в Unreal Blueprints) или список команд с drag-and-drop?
- Нужна ли возможность предпросмотра катсцены прямо в редакторе или только при запуске игры?
- Какие инструменты должны быть в редакторе? (добавление команд, копирование, удаление, изменение порядка, настройка таймингов)


### Формат выходного файла

- В каком формате сохранять: JSON, XML, собственный текстовый формат или бинарный?
- Файлы должны быть человекочитаемыми для ручного редактирования или это не важно?
- Нужна ли компрессия или шифрование данных?


### Технические детали

- Редактор должен быть частью игры (внутриигровой редактор) или отдельным приложением?
- Если внутриигровой - нужен ли он только в debug-режиме или и в релизе?
- Какие ресурсы игры должны быть доступны в редакторе? (спрайты, звуки, объекты, комнаты)

🧠 Дополнительно стоит подумать о масштабируемости системы - планируешь ли ты делать простые катсцены (пара диалогов) или сложные синематики с множеством параллельных событий?

Ответь на эти вопросы, и я помогу составить детальный промпт для генерации системы редактора катсцен!

---

# Диалоги, камера, анимации, эффекты, звуки, может что-то еще

с возможностью обоих вариантов
система условий и ветвлений нужна

я бы выбрал нодовый редактор с возможностью drag-and-drop и
нужен предпросмотр катсцены в редакторе
все эти перечисленные инструменты нужны  + возможность нарисовать путь (с опцией выравнивания линий и отклонений) + возможность импорта комнат\файлов\объектов из проекта gamemaker (спрайты и анимации в том числе), на первую версию хватит

выходной формат - json
файлы должны быть человекочитаемые, и с четким синтаксисом. сейчас я добавлю тебе файл, как реализована наша система - может станет понятнее
редактор - отдельное приложение

Отлично! Изучил структуру твоей системы. Теперь составлю подробный промпт для нейросети:[^2_1]

***

## ПРОМПТ ДЛЯ ГЕНЕРАЦИИ ВИЗУАЛЬНОГО РЕДАКТОРА КАТСЦЕН

### КОНТЕКСТ ПРОЕКТА

Необходимо создать **отдельное десктопное приложение** - визуальный редактор катсцен для GameMaker проекта с существующей action-based системой катсцен. Редактор должен экспортировать катсцены в JSON-файлы с человекочитаемым и четким синтаксисом, совместимым с текущей архитектурой проекта.

### АРХИТЕКТУРА ЦЕЛЕВОЙ СИСТЕМЫ

**Существующая система работает так:**

- Катсцена = очередь действий (array of structs) в менеджере `obj_cutsceneManager`
- Каждое действие = struct с методами `start()`, `update()`, `cleanup()`
- Менеджер последовательно выполняет действия: вызывает start, потом каждый кадр update, при завершении cleanup
- Поддерживаются параллельные действия через `ActionParallel` и ветвления через `ActionBranch`
- Актеры (куклы персонажей) управляются через `actor_map` (имя -> instance)


### ТИПЫ ДЕЙСТВИЙ ДЛЯ ПОДДЕРЖКИ

**Базовые действия:**

1. **wait** - Задержка (параметры: `seconds`)
2. **actor_create** - Создание актера (параметры: `name`, `x`, `y`, `sprite?`, `copy_from?`)
3. **actor_destroy** - Удаление актера (параметры: `name`)
4. **move** - Движение актера (параметры: `name`, `target_x`, `target_y`, `speed?`, `use_collision?`)
5. **animate** - Анимация актера (параметры: `name`, `sprite?`, `image_index?`, `image_speed?`)
6. **dialogue** - Диалог (параметры: `node`, `dialogue_file?`)
7. **run_function** - Вызов функции (параметры: `function_name`, `args?`)

**Камера:**
8. **camera_center** - Мгновенная центровка (параметры: `x`, `y`)
9. **camera_pan** - Плавное движение (параметры: `x`, `y`, `seconds`)
10. **camera_track** - Следование за актером (параметры: `actor_name`, `seconds`, `offset_x?`, `offset_y?`)
11. **camera_track_until_stop** - Следование до остановки актера (параметры: `actor_name`, `offset_x?`, `offset_y?`)

**Управление потоком:**
12. **parallel** - Параллельное выполнение (параметры: `actions[]`)
13. **branch** - Условное ветвление (параметры: `condition_id`, `then_actions[]`, `else_actions[]`)

### ТРЕБОВАНИЯ К ИНТЕРФЕЙСУ

**Тип редактора:** Нодовый (node-based) редактор с drag-and-drop

**Структура:**

- Главная рабочая область с бесконечным холстом (infinite canvas) для размещения нод
- Ноды соединяются визуальными связями для определения последовательности выполнения
- Каждая нода = одно действие из списка выше

**Панель инструментов:**

- Палитра всех типов действий для drag-and-drop на холст
- Инструмент рисования пути движения с функциями:
    - Свободное рисование кривых
    - Опция выравнивания линий (snap to grid/angles)
    - Возможность задать отклонения от прямой
    - Конвертация нарисованного пути в массив координат для `move` действий
- Кнопки управления: копировать, вставить, удалить, отменить/повторить

**Система подключения нод:**

- Каждая нода имеет входящий порт (сверху) и исходящий порт (снизу)
- Для `parallel` - несколько исходящих портов
- Для `branch` - два исходящих порта (true/false)
- Drag-and-drop соединений между портами

**Свойства нод:**

- При клике на ноду справа открывается панель свойств
- Поля ввода для всех параметров действия
- Валидация обязательных полей
- Dropdown для выбора актеров (из созданных выше по цепочке)
- Автокомплит для имен ресурсов


### ИМПОРТ РЕСУРСОВ ИЗ GAMEMAKER

**Необходимая функциональность:**

- Браузер файлов проекта GameMaker (.yyp)
- Парсинг структуры проекта для извлечения:
    - Спрайтов (sprites) - имя, preview первого кадра
    - Объектов (objects) - имя, спрайт по умолчанию
    - Комнат (rooms) - имя, размеры, preview
- Отображение библиотеки ресурсов в боковой панели с поиском
- Drag-and-drop ресурсов из библиотеки:
    - Спрайт -> в поле `sprite` ноды
    - Объект -> в поле `copy_from` ноды actor_create
    - Комната -> для отображения background в preview

**Формат хранения:**

- В JSON сохраняются только имена ресурсов как строки
- Резолв в asset_id происходит на стороне GameMaker


### СИСТЕМА ПРЕДПРОСМОТРА

**Встроенный preview-режим:**

- Кнопка "Play" для запуска предпросмотра текущей катсцены
- Окно предпросмотра показывает:
    - Виртуальную комнату (размер view_camera)
    - Актеров с их спрайтами и позициями
    - Движение камеры
    - Таймлайн выполнения внизу (текущее действие подсвечено)
- Возможность паузы, остановки, перемотки
- Скорость воспроизведения (0.5x, 1x, 2x)

**Визуализация в предпросмотре:**

- Спрайты актеров (если импортированы, иначе placeholder квадрат)
- Траектории движения (пути из `move` действий)
- Зона видимости камеры
- Временные метки событий


### ФОРМАТ ВЫХОДНОГО JSON

**Структура файла:**

```json
{
  "version": 1,
  "cutscene_id": "название_катсцены",
  "metadata": {
    "created": "ISO timestamp",
    "modified": "ISO timestamp",
    "author": ""
  },
  "actions": [
    {
      "type": "actor_create",
      "name": "player_actor",
      "x": 100,
      "y": 200,
      "sprite": "spr_player_idle"
    },
    {
      "type": "wait",
      "seconds": 1.5
    },
    {
      "type": "parallel",
      "actions": [
        {
          "type": "move",
          "name": "player_actor",
          "target_x": 300,
          "target_y": 200,
          "speed": 2
        },
        {
          "type": "camera_pan",
          "center_x": 300,
          "center_y": 200,
          "seconds": 2
        }
      ]
    },
    {
      "type": "branch",
      "condition": "check_player_health",
      "then_actions": [
        {
          "type": "dialogue",
          "node": "player_healthy",
          "dialogue_file": "scene_01.yarn"
        }
      ],
      "else_actions": [
        {
          "type": "dialogue",
          "node": "player_injured"
        }
      ]
    }
  ]
}
```

**Требования к синтаксису:**

- Отступы: 2 пробела
- Опциональные параметры: если не заданы, не включаются в JSON
- Валидация на выходе: обязательные поля присутствуют, типы корректны
- Комментарии: возможность добавить поле `"_comment"` к любому действию (игнорируется при загрузке)


### ТЕХНИЧЕСКИЕ СПЕЦИФИКАЦИИ

**Стек технологий (рекомендации):**

- Electron или Tauri для кросс-платформенного десктоп приложения
- React Flow или Rete.js для нодового редактора
- Monaco Editor или CodeMirror для просмотра/редактирования JSON напрямую
- Canvas API или Pixi.js для preview-режима

**Требования к производительности:**

- Поддержка катсцен до 200+ нод без лагов
- Плавное масштабирование холста (zoom/pan)
- Автосохранение каждые 30 секунд
- История отмены до 50 действий

**Дополнительные функции:**

- Поиск по нодам (Ctrl+F)
- Мини-карта холста в углу
- Группировка нод в коллапсируемые блоки
- Экспорт в PNG схемы катсцены
- Темная/светлая тема
- Горячие клавиши:
    - Space + drag = навигация по холсту
    - Delete = удалить выбранные ноды
    - Ctrl+D = дублировать
    - Ctrl+G = группировать
    - Ctrl+S = сохранить
    - Ctrl+E = экспорт JSON


### ПРИОРИТЕТ ФУНКЦИЙ ДЛЯ ПЕРВОЙ ВЕРСИИ

**Must have (v1.0):**

1. Нодовый редактор с всеми 13 типами действий
2. Drag-and-drop создание и соединение нод
3. Панель свойств для редактирования параметров
4. Экспорт в JSON с корректной структурой
5. Импорт JSON обратно в редактор
6. Инструмент рисования путей движения
7. Базовый импорт списка ресурсов GameMaker (sprites, objects)
8. Предпросмотр катсцены с визуализацией актеров и камеры

**Nice to have (v1.1+):**

- Парсинг комнат с отображением фона
- Расширенный preview с анимацией спрайтов
- Мультиязычность интерфейса
- Облачное хранение проектов
- Коллаборация в реальном времени


### БЕЗОПАСНОСТЬ И ВАЛИДАЦИЯ

**При загрузке JSON:**

- Проверка версии формата
- Валидация всех обязательных полей
- Проверка типов данных
- Детектирование циклических зависимостей в branch/parallel
- Предупреждения о несуществующих ресурсах

**Ограничения:**

- `run_function`: только из whitelist разрешенных функций (редактируется в настройках)
- Максимальная глубина вложенности parallel/branch: 10 уровней
- Лимит на количество действий в одном parallel: 20

***

🧠 **Дополнительные соображения по архитектуре:**

Учитывая твой опыт с GML и то, что система уже работает в проекте, я бы рекомендовал сделать редактор максимально независимым от рантайма игры. Он должен быть «тупым» визуальным конструктором JSON, а вся логика выполнения остается в твоей существующей системе на GML.

Для первой версии можно упростить preview - вместо полноценного рендера спрайтов, делай схематичную визуализацию (квадраты для актеров, стрелки для движения). Это сэкономит время разработки, а полноценный preview можно будет добавить позже, когда основной функционал работает.

Также стоит подумать о реверс-инжиниринге: возможность загрузить существующий катсценарий из GML кода (scr_cutscene_test) и автоматически построить граф нод. Это упростит миграцию текущих сцен в редактор.

**Готов составить более детальный технический документ по какому-то конкретному компоненту?** (например, детальная спецификация парсера .yyp, архитектура preview-системы, или формат промежуточного представления для инструмента рисования путей)

<div align="center">⁂</div>

[^2_1]: cutscene_system_ai_context_ru.txt

